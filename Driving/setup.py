from setuptools import setup

setup(
    name="driving",
    version='0.0.1',
    install_requires=['gymnasium',
                      'pybullet',
                      'numpy',
                      'matplotlib',
                      'torch']
)
